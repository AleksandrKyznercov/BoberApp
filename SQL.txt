CREATE TABLE Customer(
       ID_Customer      INTEGER AUTO_INCREMENT PRIMARY KEY,
       FIO			    VARCHAR(220),
       passSerialNumber VARCHAR(220),
       passNumber       VARCHAR(220),
       vidan 	    	VARCHAR(220),
       adressProp		VARCHAR(220),
       adressFact 		VARCHAR(220),
       phone	    	VARCHAR(220)
) ENGINE = MYISAM;

CREATE TABLE Treaty(
       ID_Treaty        INTEGER AUTO_INCREMENT PRIMARY KEY,
       ID_Customer      INTEGER,
       Start	 	    DATE,
       Stop	            INTEGER,
       Status			VARCHAR(20)
) ENGINE = MYISAM;

CREATE TABLE Аction(
       ID_Action		INTEGER AUTO_INCREMENT PRIMARY KEY,
	   ID_Equipment		INTEGER,
	   ID_Treaty		INTEGER,
	   Cost				FLOAT,
	   RentTime			VARCHAR(20),
	   
) ENGINE = MYISAM;
